name: "dbt_hr"
version: "1.0.0"
config-version: 2

profile: "dbt_hr"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_hr:
    staging:
      +schema: staging
      +materialized: view
    transformed:
      +schema: transformed
      +materialized: table
    marts:
      +schema: marts
      +materialized: view
